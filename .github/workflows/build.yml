name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: config         # where your split_left.overlay + split_right.overlay live
      # build_matrix_path: build.yaml  # optional, defaults to "build.yaml"
      # archive_name: firmware         # optional
      # fallback_binary: bin            # optional, defaults to "bin"
